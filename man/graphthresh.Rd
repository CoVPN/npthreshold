% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphthresh.R
\name{graphthresh}
\alias{graphthresh}
\title{Plot threshold-response estimates}
\usage{
graphthresh(
  res,
  type = "raw",
  ylabel = "Estimate",
  xlabel = "Thresholds",
  cutoffs = NULL,
  exp10 = FALSE,
  plot_density = FALSE,
  plot_endpoints = FALSE,
  data = NA,
  weights = NA,
  marker = NA,
  annotate = NA,
  event = NA,
  time_var = NA,
  tf = NA,
  ylimit = NULL,
  xlimit = NULL,
  yright_endpoint = "min"
)
}
\arguments{
\item{res}{A \code{data.frame} obtained from \code{thresholdSurv()} or
\code{thresholdBinary()}.}

\item{type}{Either \code{"raw"}, \code{"monotone"}, or \code{"ve"}.}

\item{ylabel}{Label for the y-axis.}

\item{xlabel}{Label for the x-axis.}

\item{cutoffs}{A numeric vector \code{c(lower, upper)} used to clip the lower
and upper confidence bounds, respectively. The point estimates are not
clipped.}

\item{exp10}{If \code{TRUE}, label x-axis values as powers of ten.}

\item{plot_density}{If \code{TRUE}, add the weighted reverse cumulative
distribution function (reverse CDF) of the marker.}

\item{plot_endpoints}{If \code{TRUE}, add points for individual disease
endpoints.}

\item{data}{Original dataset. Required when \code{plot_density = TRUE},
\code{plot_endpoints = TRUE}, or an annotation is supplied, and when
\code{exp10 = TRUE} without \code{xlimit}.}

\item{weights}{Name of the weight variable in \code{data}. Required when
\code{plot_density = TRUE}.}

\item{marker}{Name of the marker variable in \code{data}. Required for
density or endpoint plotting and annotations, and when \code{exp10 = TRUE}
without \code{xlimit}.}

\item{annotate}{String for text to annotate in the corner of the graph}

\item{event}{Name of the event variable in \code{data}. Required when
\code{plot_endpoints = TRUE}.}

\item{time_var}{Name of the time-to-event variable in \code{data}. Required
when \code{plot_endpoints = TRUE}.}

\item{tf}{Reference time point used to select endpoints occurring on or
before that time. Required when \code{plot_endpoints = TRUE}.}

\item{ylimit}{Numeric vector \code{c(min, max)} for y-axis limits, optionally
followed by a positive tick step: \code{c(min, max, tick_step)}.}

\item{xlimit}{Numeric vector \code{c(min, max)} for x-axis limits, optionally
followed by a positive tick step: \code{c(min, max, tick_step)}. When the
step is omitted, tick locations are chosen automatically.}

\item{yright_endpoint}{Either \code{"min"} or \code{"max"}; controls whether endpoint
interpolation beyond the observed threshold range uses the minimum or
maximum estimate.}
}
\value{
A \code{ggplot2} object displaying the estimates and confidence
intervals for \eqn{E[E[Y|A \ge v, W]]} across the specified thresholds.
}
\description{
This function creates a plot of estimates and confidence intervals for
\eqn{E[E[Y|A \ge v, W]]} across various thresholds.
}
\examples{
estimates <- data.frame(
  threshold = 1:3,
  estimate = c(0.20, 0.25, 0.31),
  ci_lo = c(0.15, 0.19, 0.24),
  ci_hi = c(0.25, 0.31, 0.38)
)
graphthresh(estimates, xlimit = c(1, 3))
graphthresh(estimates, xlimit = c(1, 3, 0.5))
}
